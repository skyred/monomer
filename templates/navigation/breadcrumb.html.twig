{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {% polymer import "x-breadcrumb.html" %}
  <x-breadcrumb breadcrumb="{{ breadcrumb | json_encode | e('html_attr') }}"
                aria-labelledby="system-breadcrumb"
                role="navigation"
                class="breadcrumb"
                heading="{{'Breadcrumb'|t| e('html_attr')}}">
  </x-breadcrumb>
{% endif %}
